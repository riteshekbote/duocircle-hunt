[0m
> build · mimo-v2.5-free
[0m
Looking at the 17 grep hits, here's my classification:

## Classification Table

| # | File:Line | Pattern | Classification | Rationale |
|---|-----------|---------|----------------|-----------|
| 1 | `youtrack-mcp/scripts/test-field-values.js:17` | `baseURL: 'https://test.youtrack.cloud'` | ENDPOINT_LEAK | Internal YouTrack instance reference (matches known `youtrack-mcp` → internal YouTrack inventory item) |
| 2 | `youtrack-mcp/scripts/test-field-values.js:18` | `token: 'test-token'` | TEST_OR_EXAMPLE | Literal string `test-token`, not a real credential |
| 3 | `youtrack-mcp/scripts/test-error-handling.js:15` | `baseURL: 'https://test.youtrack.cloud'` | ENDPOINT_LEAK | Same as #1 |
| 4 | `youtrack-mcp/scripts/test-error-handling.js:16` | `token: 'test-token'` | TEST_OR_EXAMPLE | Same as #2 |
| 5-13 | `dmarcreport-mcp/` (package.json, docker-compose.yml, src/index.ts, src/tools/help.ts) | Various `dmarcreport` references | TEST_OR_EXAMPLE | Package naming, defaults, documentation URLs — all expected for a first-party MCP server |
| 14 | `dmarcreport-mcp/src/guidance/patterns.ts:398` | `pattern: /include:_s\d+\.autospf\.email/i` | INTERESTING | Pattern-matching regex for autospf.email subdomains |
| 15 | `dmarcreport-mcp/src/guidance/patterns.ts:400` | `dashboard_url: "https://autospf.com/dashboard"` | INTERESTING | Hardcoded dashboard URL in guidance pattern |
| 16 | `dmarcreport-mcp/src/guidance/patterns.ts:404` | `pattern: /include:.*\.autospf\.com/i` | INTERESTING | Pattern-matching for autospf.com subdomains |
| 17 | `dmarcreport-mcp/src/index.ts:33` | `const API_URL = ... 'https://api.dmarcreport.com'` | TEST_OR_EXAMPLE | Default API URL, already known in inventory |

## Verdict List

| Candidate | REPORT_CANDIDATE | Rationale |
|-----------|-----------------|-----------|
| `youtrack-mcp` test scripts referencing `test.youtrack.cloud` | **no** | `test.youtrack.cloud` is a generic YouTrack Cloud test endpoint (not an internal self-hosted instance). The `token: 'test-token'` values are literal dummy strings. No real secret leaked. |
| `dmarcreport-mcp` hardcoded URLs/patterns | **no** | All are first-party references to in-scope domains (`api.dmarcreport.com`, `autospf.com/dashboard`). No secrets, no internal/dev endpoints beyond known inventory. |
| `dmarcreport-mcp` package naming (`@duocircle/dmarcreport-mcp`) | **no** | First-party scoped package on npm — expected for their MCP server distribution. |

## Summary

**REPORT_CANDIDATE: no** — All 17 hits are either test fixtures (`test-token`), first-party package naming/documentation, or references to already-inventoried endpoints. The `youtrack-mcp` scripts use `test.youtrack.cloud` which is YouTrack Cloud's public test domain, not an internal instance. No credentials, no internal staging/dev secrets, no actionable findings.
